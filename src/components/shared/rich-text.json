{
  "collectionName": "components_shared_rich_texts",
  "info": {
    "displayName": "Rich text",
    "icon": "align-justify",
    "description": ""
  },
  "options": {},
  "attributes": {
    "body": {
      "type": "richtext"
    },
    "spacing": {
      "type": "enumeration",
      "enum": [
        "none",
        "top",
        "bottom",
        "both"
      ],
      "default": "none"
    },
    "background": {
      "type": "enumeration",
      "enum": [
        "white",
        "gray"
      ],
      "default": "white"
    },
    "css_class": {
      "type": "string"
    },
    "css_id": {
      "type": "string"
    },
    "visibility": {
      "type": "enumeration",
      "enum": [
        "all",
        "logged_in",
        "not_logged_in"
      ],
      "default": "all"
    },
    "render_mode": {
      "type": "enumeration",
      "enum": [
        "append",
        "override"
      ],
      "default": "append"
    }
  }
}
